import type { LevelProp } from '../types/LevelProp.interface'

const Level3 = ({ propValue }: LevelProp) => {
  return (
    <div>
      <h4>Level3: {propValue}</h4>
    </div>
  )
}

export default Level3
